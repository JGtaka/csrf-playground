# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

**CSRF学習シミュレーター** — クロスサイト・リクエスト・フォージェリ（CSRF）の仕組みを、ストーリー形式の体験を通じて初学者が直感的に理解できる教育用Webアプリ。架空のSNS「Chirpy」を題材にする。

仕様書は `README.md` を一次情報として参照すること。本ドキュメントは仕様書を補完する開発者向けガイドである。

## 現在の状態

リポジトリは**仕様策定済み・実装未着手**の段階。`Dockerfile` と `compose.yml` は空ファイルで、ソースコード（`package.json`, `src/` 等）はまだ存在しない。実装に着手する際は、まず Vite + React 19 + TypeScript のテンプレートからセットアップする必要がある。

## 技術スタック（確定事項）

- React 19
- React Router 7
- TypeScript
- CSS Modules（グローバルCSSやTailwind等は使わない方針）
- Vite
- Docker

## アーキテクチャ方針

### 画面構成は2画面のみ
1. **トップページ**（ランディング） — ヘッダー / ヒーロー / メッセージ / フッター
2. **シミュレーターページ**（ストーリー型） — 中央スマホフレーム＋左右解説カードの3カラム

サイドバーや上部ナビゲーションは**意図的に持たない**。シンプルな構成を維持すること。

### ステップ駆動の状態設計
シミュレーターページは4ステップ（Authentic Login → Visit Trap Site → Forged Request → Damage & Defense）の状態機械として設計する。

- 中央スマホ表示／左カード（解説）／右カード上（HTTP Request等の技術詳細）／右カード下（Countermeasures）の **4領域すべてが現在ステップに同期して切り替わる**
- 右カードの内容は**ステップごとに切り替える**こと。全ステップ共通の固定表示にしてはいけない（例：Step 1 ではCookieの保存内容、Step 3 では偽装リクエストの中身、Step 4 では防御策）
- 進行は**順方向のみ**（Next Step）。Resetで Step 1 に戻る
- ステップ切り替え時は短いフェード・スライドアニメーションで連続性を保つ

### 言語ポリシー
UIは日本語。**コード例／HTTPヘッダ／技術用語は英語のまま**表示する（学習者が実物に触れた時に違和感がないように）。

## コーディング規約

- インデントは2スペース
- JS/TSのセミコロンは省略
- 変数名・関数名は英語、コメントは日本語

## コミット規約

- コミットメッセージは**日本語**
- Conventional Commits形式（`feat:`, `fix:`, `docs:` 等）
- **mainブランチへの直接コミットは禁止**。フィーチャーブランチで作業する
- pushする前に必ずユーザーに確認を取る

## 非機能要件で意識すべきこと

- レスポンシブはデスクトップ優先・タブレットまで。スマホ実機ではスマホフレーム比率を縮小表示
- アクセシビリティ：ボタンに `aria-label`、キーボード操作可能
- 画像は最適化し、初期ロードを軽量に保つ

## フェーズ2候補（現時点では実装しない）

クイズ機能、防御策ON/OFFトグル、修了バッジ、ダークモード — これらは仕様確定前なので、フェーズ1の実装中に先回りして基盤を作り込まないこと。
